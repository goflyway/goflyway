version: '3'

services:
  mysql:
    image: 'mysql/mysql-server:latest'
    ports:
      - "9910:3306"
    environment:
      MYSQL_DATABASE: goflyway
      MYSQL_ROOT_PASSWORD: goflyway
      MYSQL_ROOT_HOST: "%"